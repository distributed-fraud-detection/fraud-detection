# Test profile — Testcontainers provides REAL PostgreSQL, Kafka and Redis.
# URLs/ports are overridden at runtime via @DynamicPropertySource in AbstractIntegrationTest.
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop # fresh schema each test run
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.frauddetection: DEBUG
    org.springframework.kafka: WARN
